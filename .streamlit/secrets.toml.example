# Streamlit secrets 템플릿 — 실제 값은 커밋하지 말 것.
# 로컬: 이 파일을 .streamlit/secrets.toml 로 복사하고 값을 채운다.
# Streamlit Cloud: 앱 설정 > Settings > Secrets 에 아래 두 줄을 붙여넣는다.

APP_PASSWORD = "팀에서 공유할 접속 비밀번호"
ANTHROPIC_API_KEY = "sk-ant-..."
